package com.github.dach83.sharedtestcode.models

import com.github.dach83.bin.core.domain.model.CardQuery

val visaCardQuery = CardQuery(
    cardNumber = CardNumbers.VISA
)

val masterCardQuery = CardQuery(
    cardNumber = CardNumbers.MASTER_CARD
)
